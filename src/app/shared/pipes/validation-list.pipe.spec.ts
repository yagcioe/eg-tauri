import { ValidationListPipe } from './validation-list.pipe';

describe('ValidationListPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidationListPipe();
    expect(pipe).toBeTruthy();
  });
});
